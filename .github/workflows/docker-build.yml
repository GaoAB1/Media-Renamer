# ═══════════════════════════════════════════════════════════════
# Media Renamer — Docker 镜像自动构建发布（GHCR only）
# 触发：
#   · push 到 master/main → 构建 latest + 推送 GHCR
#   · 打 v* 标签（如 v1.0.0）→ 构建对应版本 tag
#   · 手动 workflow_dispatch 也可触发
# GHCR 认证（二选一，优先 PAT）：
#   · 仓库 Secret GHCR_PAT（Fine-grained PAT，勾选 write:packages）
#   · 或 GITHUB_TOKEN（需 Settings→Actions→Workflow permissions 设为 Read and write）
# ═══════════════════════════════════════════════════════════════
name: buildx

on:
  push:
    branches: [master, main]
    tags: ['v*']
  workflow_dispatch:

jobs:
  buildx:
    runs-on: ubuntu-latest
    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 设置 QEMU（多架构模拟）
        uses: docker/setup-qemu-action@v3

      - name: 设置 Buildx
        uses: docker/setup-buildx-action@v3

      - name: 登录 GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_PAT || secrets.GITHUB_TOKEN }}

      - name: 生成镜像标签
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=ref,event=branch,suffix=-dev
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest,enable={{is_default_branch}}

      - name: 构建并推送（多架构）
        uses: docker/build-push-action@v5
        with:
          context: .
          platforms: linux/amd64,linux/arm64,linux/arm/v7
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
